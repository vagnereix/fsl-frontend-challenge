export const findLessCostPath = (board: number[][]): number => {
  let lessCostPath: number = 0;

  const numRows = board.length;
  const numCols = board[0].length;

  for (let i = 0; i < numRows; i++) {
    for (let j = 0; j < numCols; j++) {}
  }

  return lessCostPath;
};
